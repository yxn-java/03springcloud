server:
  port: 1410
spring:
  application:
    name: spring-cloud-user-service-consumer3
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1110/eureka
    register-with-eureka: true
    fetch-registry: true
